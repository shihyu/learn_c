/* strlen example */
#include <stdio.h>
#include <string.h>

int main ()
{
    int i;
  char szInput[4] = {'A','C','B','\0'};


  for (i = 0; i < strlen(szInput); ++i)
  {
      printf("%c\n",szInput[i]);
      /* code */
  }
  return 0;
}
