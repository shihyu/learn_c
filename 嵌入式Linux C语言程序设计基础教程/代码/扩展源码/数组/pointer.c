#include <stdio.h>

int main()
{
	int a[] = {1, 4, 3, 6};
	int i;

/*
	int *p;
	p = &a[0];
	
	p += 2;
	printf("p[1] = %d\n", p[1]);//*(p+1)
		
	
	for(i = 0; i < sizeof(a) / sizeof(int); i++)
		printf("%d  %d  %d  %d  ", a[i], *(a+i), *(p+i), p[i] );
	printf("\n");

	printf("%p, %p", a, a+1);
	printf("\n");
*/
	return 0;
}
