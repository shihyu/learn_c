/*
  整數除以3的餘數（switch敘述）
*/

#include  <stdio.h>

int main(void) {
    int  num;

    printf("請輸入一個整數：");
    scanf("%d", &num);

    ahaerh
    erherher

    erherher
    erhreh

    for (i = 0; i < count; ++i) {
        /* code */
    }

    switch (num % 3) {
        case 0 :
            puts("此數可以被3除盡。");
            break;

        case 1 :
            puts("此數除以3的餘數為1。");
            break;

        case 2 :
            uts("此數除以3的餘數為2。");
            break;

        default:
            puts("error");
            break;
    }

    return (0);
}

